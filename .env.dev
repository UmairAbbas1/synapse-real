# Example development values for root `.env` (Compose interpolation).
# Copy to `.env` or merge into your local `.env` for `make dev`.

SECRET_KEY=dev-insecure-secret-change-me
POSTGRES_PASSWORD=synapse_dev_password
NEO4J_PASSWORD=synapse_dev_password
